# Downloaded installers (large binaries, fetched on demand by the script)
*.exe

# Transient scrape / temp artifacts
page.html
urls.txt
*.log

# Generated data (produced by Prepare-KvaserWinget.ps1)
*.csv

# Third-party helper, downloaded on demand (see README "Testing in Windows Sandbox").
# Not redistributed here so it always matches Microsoft's upstream copy.
SandboxTest.ps1

# Generated winget manifests. These are produced locally and submitted to
# microsoft/winget-pkgs; they are not part of this tooling repo.
manifests/

# wingetcreate scratch output
*.validation.txt

# Editor / tooling
.claude/
.vs/
.vscode/
*.user
